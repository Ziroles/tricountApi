services:
  tricount-api:
    build: .
    container_name: tricount-api
    restart: unless-stopped
    ports:
      - "${TRICOUNT_RELAY_PORT:-8787}:8787"
    volumes:
      - tricount-data:/data
    environment:
      - TRICOUNT_RELAY_HOST=0.0.0.0
      - TRICOUNT_RELAY_PORT=8787
      - TRICOUNT_RELAY_KEY=${TRICOUNT_RELAY_KEY:-}


volumes:
  tricount-data:
