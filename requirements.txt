# API SplitTicket — dépendances directes.
# Les versions sont figées : une API de lecture de tickets n'a rien à gagner à
# suivre l'amont automatiquement, et beaucoup à perdre si un client non officiel
# change de comportement du jour au lendemain.

fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.5

# Lecture des tickets.
google-genai==2.22.0

# Client Tricount non officiel, rétro-conçu depuis l'application Android.
# Apporte aussi `cryptography`, dont on se sert pour chiffrer au repos les clefs
# Gemini que les utilisateurs nous confient.
tricount-api==0.1.2
cryptography==50.0.1
